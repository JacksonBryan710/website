# Copy this file to .env.local (already gitignored) and fill in your own key.
# Used by the Letterboxd/Goodreads widgets on the Now page to raise rss2json's
# rate limit and unlock the `count` param. Get a free key at https://rss2json.com/
VITE_RSS2JSON_API_KEY=
